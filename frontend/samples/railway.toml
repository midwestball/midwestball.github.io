[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python data_collection.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[variables]
PYTHONPATH = "/app"
PORT = "8000"